export function getPlayers(gameState) {
//     const players = [];
//     for (const k in gameState.players) {
//         const p = gameState.players[k];
//         players[parseInt(k) - 1] = {cards};
//     }
//     return players;
}
